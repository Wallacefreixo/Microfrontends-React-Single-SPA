import { Button } from "@Wallacefreixo/library-components-react";
import "./style.css";

function App(){
  return (
    <div className="box-app">
      <Button 
        color="primary" 
        onClick={() => {} }
      >
        Primary Button
      </Button>
    </div>
  );
}

export default App;